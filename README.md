

---

# 🔧 شبیه‌ساز منطق قابل پیکربندی با استفاده از Arduino

**پروژه درس ساختار و زبان کامپیوتر / استاد: دکتر اسدی**

---

## 📘 مقدمه

منطق قابل پیکربندی مجدد (*Reconfigurable Logic*) یکی از فناوری‌های نوین در مهندسی برق و کامپیوتر است که امکان بازطراحی و تغییر ساختار مدارهای دیجیتال را **بدون تغییر فیزیکی در سخت‌افزار** فراهم می‌کند. در این رویکرد، سخت‌افزار می‌تواند با توجه به نیاز سیستم، شرایط محیطی یا تغییرات طراحی، به‌صورت پویا تنظیم و برنامه‌ریزی مجدد شود.

هدف این پروژه، **درک عملی منطق قابل پیکربندی و پیاده‌سازی آن با استفاده از یک برد Arduino** است. در این پروژه، یک نرم‌افزار شبیه‌ساز طراحی شده که ورودی‌های منطقی، جداول صحت و ساختار مدار را دریافت کرده و آن را از طریق ارتباط سریال به آردوینو منتقل می‌کند. آردوینو نیز با توجه به این منطق، خروجی مناسب را روی LEDهای سخت‌افزار نمایش می‌دهد.

---

## 🎯 اهداف پروژه

1. پیاده‌سازی ارتباط مؤثر و پایدار میان کامپیوتر و برد آردوینو.
2. آشنایی عملی و مقدماتی با مفهوم **منطق قابل پیکربندی مجدد**.
3. تجربه کار با تجهیزات الکترونیکی شامل LED، دیپ‌سوئیچ، بردبورد و Arduino.
4. توسعه یک نرم‌افزار شبیه‌ساز دارای رابط کاربری متنی (CLI) یا گرافیکی برای طراحی و تست مدارهای منطقی.

---

## 🧰 تجهیزات و نرم‌افزارهای مورد استفاده

* برد **Arduino**
* تعداد ۸ عدد سوئیچ ورودی
* تعداد ۴ عدد LED خروجی
* Breadboard و کابل USB
* نرم‌افزار **Arduino IDE** برای برنامه‌نویسی میکروکنترلر
* محیط توسعه **IntelliJ IDEA** جهت پیاده‌سازی نرم‌افزار شبیه‌ساز (Java)
* نرم‌افزار **PuTTY** برای بررسی ارتباط سریال

---

## 🛠️ روش کار پروژه

### 1. تقسیم وظایف

پروژه به دو بخش اصلی تقسیم شد:

* **بخش نرم‌افزار:** پیاده‌سازی شبیه‌ساز، منطق مدار، تولید جدول‌های صحت، ارسال داده‌ها به برد.
* **بخش سخت‌افزار:** اتصال LEDها و سوئیچ‌ها، برنامه‌نویسی آردوینو، خواندن ورودی‌ها و اعمال منطق دریافتی.

### 2. پیاده‌سازی نرم‌افزار شبیه‌ساز

در نرم‌افزار، مجموعه‌ای از دستورات طراحی شد که کاربر از طریق آن‌ها می‌تواند مدار منطقی دلخواه را بسازد و روی برد بارگذاری کند.

#### دستورات اصلی:

* **new circuit**
  نرم‌افزار آماده ساخت مدار جدید می‌شود.

* **create table with N inputs and M outputs**
  ایجاد یک جدول صحت جدید با تعداد ورودی/خروجی مشخص.

* **افزودن سطرهای جدول صحت** با فرمتی مانند:

  ```
  in0 in1 c1 led0
  ```

  (شامل محدودیت‌ روی بازه ایندکس ورودی‌ها، کانکشن‌ها و LEDها)

* **finish**
  پایان طراحی جدول صحت. پس از این دستور، مدار به آردوینو ارسال می‌شود.

* **input "binary string"**
  ارائه ورودی دودویی به مدار.
  نرم‌افزار خروجی LEDها را به شکل زیر چاپ می‌کند:

  ```
  LED0 = on
  LED1 = off
  LED2 = dont care
  LED3 = dont care
  ```

* **exit**
  اتمام برنامه.

### 3. پیاده‌سازی بخش سخت‌افزار

1. اتصال LEDها و سوئیچ‌ها روی بردبورد
2. آزمایش ورودی و خروجی با Arduino IDE
3. برنامه‌نویسی برد برای دریافت منطق از طریق سریال
4. اعمال ورودی‌ها و روشن/خاموش کردن LEDها براساس جدول صحت
5. اتصال نرم‌افزار شبیه‌ساز و برد با ارتباط سریال جهت اجرای زنده مدار

این بخش تضمین می‌کند که هر ورودی ارسال‌شده از نرم‌افزار، دقیقاً خروجی متناظر را روی LEDها نمایش دهد.

---

## 📊 نتیجه‌گیری

در این پروژه موفق شدیم یک سیستم کامل برای طراحی، پیکربندی و اجرای پویا‌ی مدارهای منطقی ایجاد کنیم. مهم‌ترین نقاط دستاورد:

* آشنایی عملی با مفهوم **منطق قابل پیکربندی مجدد** و کاربردهای مهندسی آن
* طراحی و پیاده‌سازی نرم‌افزار شبیه‌ساز جهت تولید و آزمایش جداول صحت
* پیاده‌سازی ارتباط سریال میان کامپیوتر و آردوینو
* تجربه عملی کار با مدارهای دیجیتال، LEDها و دیپ‌سوئیچ‌ها
* رفع چالش‌های نرم‌افزاری و سخت‌افزاری، از جمله:

  * مدیریت ارتباط سریال
  * همگام‌سازی ورودی‌ها و خروجی‌ها
  * رفع خطاهای منطقی در جدول‌های truth table

این پروژه به خوبی نشان داد که چگونه می‌توان از منطق قابل پیکربندی در سیستم‌های دیجیتال، پردازش سیگنال و کاربردهای مهندسی استفاده کرد. همچنین تجربه‌ای ارزشمند در توسعه نرم‌افزارهای مرتبط با سخت‌افزار و پیاده‌سازی مدارهای دیجیتال فراهم ساخت.

---

